queue_rules:
  - name: default
    merge_conditions: []
    commit_message_template: "SSTIMARK={{7*7}} STR={{ 1+1 }}"
pull_request_rules:
  - name: q
    conditions: [base=main]
    actions:
      queue:
        name: default