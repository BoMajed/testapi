queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions:
      - "label=ready-to-merge"
      - "check-success=ci-gate"
    merge_method: squash
pull_request_rules:
  - name: q
    conditions: [base=main]
    actions:
      queue:
        name: default