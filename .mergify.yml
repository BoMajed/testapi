pull_request_rules:
  - name: cmt
    conditions:
      - base=main
    actions:
      comment:
        message: |
          OUTSIDER-CONTROLLED via get_section: {{ body | get_section("## inj", "NADA") }}