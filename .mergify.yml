queue_rules:
  - name: default
    merge_conditions:
      - "check-success=impossible-gate-xyz"
pull_request_rules:
  - name: auto-queue
    conditions:
      - base=main
    actions:
      queue:
        name: default