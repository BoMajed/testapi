pull_request_rules:
  - name: dispatch-poc
    conditions:
      - base=main
    actions:
      github_actions:
        workflow:
          dispatch:
            - workflow: mgfpoc.yml
              inputs:
                trigger_author: "{{ author }}"
                trigger_pr: "{{ number }}"